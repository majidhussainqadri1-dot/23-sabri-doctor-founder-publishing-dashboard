<?php
/**
 * File 23 uninstall boundary.
 *
 * File 23 preserves all data by default. Native provider data is never owned
 * by this plugin and must never be deleted here. A future destructive cleanup
 * procedure, if approved, must be a separate capability-protected operation
 * with backup, confirmation, audit, and rollback documentation.
 *
 * @package Sabri_Publishing_Dashboard
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;

// Intentionally no destructive action.
